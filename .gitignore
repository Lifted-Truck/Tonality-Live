node_modules/
dist/
*.ablx
.env
__pycache__/
*.pyc

# Beta SDK tarballs are not redistributable — keep them out of version control.
extension/vendor/*.tgz

# HARNESS:START — added by /retrofit (idempotent). Machine state + local overrides.
.harness/
.claude/settings.local.json
# HARNESS:END
