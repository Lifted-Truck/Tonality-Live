# q002-tests — zero-dep unit coverage for transpose + the bridge glue

- **Queue item:** Q-002 (Real oracle coverage).
- **Why:** The oracle proved things compiled/built but not that they were
  *correct*. Added unit suites for the two pieces of logic THIS repo owns —
  the transpose transform and the bridge's JSON⇄Sequence marshalling +
  display-summary shaping — and wired them into `./verify`.
- **Evidence consulted:** extension/src/transform.ts (pure, SDK import is
  type-only); bridge/server.py (_sequence_from_payload, _summarize,
  _chord_label — the glue; mts imported at module top); SDK NoteDescription
  type (node_modules/@ableton-extensions/sdk/dist/index.d.mts:90); tsconfig
  (include src/**/*) and build.ts (entry = extension.ts only).
- **What changed:**
  - extension/src/transform.test.ts — 7 node:test cases (shift, clamp/drop,
    boundaries, identity, empty). Runs via `node --import tsx --test`.
  - bridge/test_bridge.py — 9 stdlib unittest cases; goldens pin _summarize /
    _chord_label shaping and _sequence_from_payload validation; skipUnless the
    mts engine is importable.
  - verify: fast gains node:test + py_compile of the test file; full gains the
    bridge unittest step (prefers the Tonality venv python; skips when absent).
  - ROADMAP Q-002 → done (criterion 2 honestly re-scoped: pins OUR shaping, not
    mts's analysis); status/oracle lines + decision log updated.
  - CLAUDE.md §Domain verify-targets refreshed; LIBRARY L0002 + INDEX pointer.
- **Alternatives rejected:** vitest + pytest — richer DX but adds dev deps to a
  ~411 LOC repo; human chose zero-dep. Golden of a real /analyze response —
  rejected: that tests mts's analysis (provider's domain, violates the
  no-theory-here invariant); pin our summary shaping over a fixture instead.
- **Verify:** `./verify fast` exit 0 (7 tests); `./verify full` exit 0 (build
  OK; 9 bridge tests ran against the venv engine; live contract skipped, no
  server). Recorded in .harness/last-verify.json.
- **Open questions:** none. End-to-end-in-Live coverage remains out of scope.
