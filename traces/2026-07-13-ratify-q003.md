# ratify-q003 — accept Tonality's conform rulings; confirm the provider is `mts`

- **Queue item:** Q-003 (Scale-conform via the /transform seam) — ratification
  step, not implementation. Cites traces/2026-07-13-retrofit.md (brief filed).
- **Why:** Tonality answered brief tonality-live-001 (ACCEPT the conform family,
  DEFER revoice). Ratifying returns the ball to the provider so they can
  implement + notice. The human separately asked whether the provider should be
  `tonality-core` rather than the Python engine; that had to be settled first,
  because it would have invalidated the channel.
- **Evidence consulted:**
  - `Tonality/integrations/Tonality-Live/response.md` (the five rulings).
  - `tonality-core/README.md` — Python engine is "the spec's source of truth";
    scope is only the frozen set-class identity substrate; analysis/temporal/
    rules/search/pattern layers stay Python-only until they freeze.
  - `tonality-core/include/tonality/` — headers are bitmask, chirality, dft,
    json_format, setclass, table. Grep of include/ bindings/ tools/ for
    conform|fit_to_key|midi_file_analysis|voice_lead|key_induc → no code hits.
  - `Tonality/CPP_PORT.md:79-83` — "the MCP/bridge tool surfaces" are deferred
    behind the Phase 6 fence (the surface our bridge consumes).
  - `Tonality/ROADMAP.md:1704` — our request already recorded as Phase 7
    "note-transform slice 0, accepted from Tonality-Live brief-001".
  - `Tonality/ROADMAP.md:1711-1716` — their impossibility result, which
    corroborates the collision finding.
  - Arithmetic check (scratch script): out-of-scale pcs that are equidistant
    between two scale members — C major 5/5, whole-tone 6/6, harmonic minor 3/5,
    pentatonic 3/7; max snap distance observed = 6 (their ≤ 6 guarantee holds).
- **What changed:** filed `ratify.md` on the provider's channel (mailbox
  exception; nothing committed in the Tonality repo). Here: DECISIONS D5
  (ratification + the two refinements) and D6 (provider is `mts`, not
  tonality-core, with the evidence); ROADMAP Q-003 rewritten (status, provider
  note, criteria 1/4 updated, open questions), status + decision-log lines;
  LIBRARY L0003 + INDEX pointer.
- **Alternatives rejected:** (a) switch to `tonality-core` as the human
  initially suggested — refused with evidence: it exposes no analysis or conform
  surface and the bridge tool surfaces are explicitly fenced off until Phase 6;
  the human ratified staying on `mts`. (b) Ratify silently without the two
  refinements — rejected: the tie-break default governs every accidental in
  diatonic use, and the accepted note-count guarantee locks in duplicate notes;
  both are cheaper to settle before the default is baked in. (c) Implement the
  snap locally to unblock — rejected, violates the core invariant.
- **Verify:** `./verify fast` exit 0 (7 extension tests; py_compile) — see
  .harness/last-verify.json. No source changed this pass; docs + exchange only.
- **Open questions:** R1 (tie_break default) and R2 (collision ruling) now sit
  with the provider. Q-003 stays blocked (upstream) until their notice. Whether
  `"down"` is ultimately the right default is theirs to rule, not ours.
