# retrofit — catch Tonality-Live up to the ecosystem harness

- **Queue item:** Q-001 (Harness retrofit)
- **Why:** The repo had real content (v1 extension + bridge) but no harness —
  no charter, oracle, ROADMAP, decision log, knowledge loop, or agent config.
  Retrofit installs the standard scaffolding non-destructively so future
  sessions orient without code-diving and every change is gated + traced.
- **Evidence consulted:** ONBOARDING.md Part 2 (retrofit procedure); the
  harness kit (`autonomous/harness/` + `kit/`); reference manifests
  (distillery, dispatch); repo source (extension/src/*.ts, bridge/server.py),
  README.md, package.json; the canonical sweep presence-checks.
- **What changed:**
  - Created: CLAUDE.md (charter + §Domain + knowledge-loop), verify (oracle
    wrapping tsc + py_compile + build + bridge smoke), ROADMAP.md, DECISIONS.md
    (D1), INDEX.md + LIBRARY.md (1 seeded lesson L0001), CODEMAP.md,
    .claudeignore, project.manifest.json (rung 1, provisional), .claude/
    (agents+hooks+settings+provenance skill, verbatim), traces/.
  - Appended (marker-delimited, idempotent): .gitignore (`.harness/`,
    `.claude/settings.local.json`); README.md (harness pointer + dated
    last-verified line). README body and all extension/ + bridge/ source
    untouched.
  - Filed a consumer brief to the Tonality engine (mailbox exception):
    `~/Documents/Tonality/integrations/Tonality-Live/brief.md`
    (id tonality-live-001, ball: provider, respond-by 2026-07-27) for the
    /transform functions blocking ROADMAP Q-003.
- **Alternatives rejected:** rung 2/3 (subagents/fleet) — not earned by a
  ~411 LOC single-dev thin client (Decision 8, burden of proof). Writing real
  test suites now — deferred to Q-002 as explicit debt rather than expanding
  retrofit scope. Not filing the brief — rejected; the consumer relationship
  is real and the seam gap is already documented, so the ball belongs with the
  provider.
- **Verify:** `./verify fast` exit 0, git c94cf67 (.harness/last-verify.json);
  `./verify full` also exit 0 (build OK; bridge check skipped-with-notice, no
  engine running — degraded per INTEGRATIONS rule 2).
- **Open questions:** manifest is provisional pending human ratification;
  Q-002 test-framework choice (vitest vs node:test) unresolved; Q-003 owned by
  the brief's respond-by. Whether the Node engine warning bites at
  Extension-Host runtime is unverified (LIBRARY L0001 falsifier).
