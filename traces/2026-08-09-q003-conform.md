# q003-conform — /transform live; Fit to Key + Conform to Scale wired

- **Queue item:** Q-003 (Scale-conform via the /transform seam) — closed.
  Cites traces/2026-07-13-ratify-q003.md (the ratification this delivers on).
- **Why:** Tonality shipped `conform_to_scale` + `fit_to_key` and returned the
  ball (`response-2.md`). Our three committed steps were: take `/transform`
  live, add its contract check to `./verify full`, confirm green on the channel.
- **Evidence consulted:**
  - `Tonality/integrations/Tonality-Live/response-2.md` (rulings + the wire).
  - `Tonality/mts/generate/conform.py` read directly — **the response's "wire"
    section says `conform_to_scale(events, …)` but the real first parameter is a
    `Sequence`.** Coded against the source, not the description.
  - Engine commit `6ee72a7` (PR #259); catalog via `mts.io.loaders.load_scales`
    (37 scales).
  - Smoke-tested the engine before designing: C-C♯-D-F♯ → C major yields
    `[60,60,62,65]`, two `tied` edits resolved `previous`, one collision
    `midi 60, source_midis [60,61]` — the case pinned in the ratification.
- **What changed:**
  - `bridge/server.py`: `/transform` live (op dispatch, `_notes_from_events`),
    `GET /scales`, `NotImplementedError` → 501 so `revoice` stays visibly absent.
  - `extension/src/bridge.ts`: shared `request`/`postJson` (the "start the
    bridge" advice now lives once), `transform`, `fetchScales`, wire types.
  - `extension/src/transform.ts`: `dedupeCollisions` — pure, type-only imports
    kept so node:test still runs without the SDK runtime (LIBRARY L0002).
  - `extension/src/extension.ts`: `tonality.fitToKey`, `tonality.conformToScale`,
    shared `runConform` (one undo step via `withinTransaction`), result copy.
  - `extension/ui/{fit-to-key,conform-to-scale}.html`; `analysis.html` renders a
    plain `note`.
  - `verify`: `/transform` contract check. Tests: +6 extension, +10 bridge.
  - Docs: README (4 actions, dated last-verified), bridge/README (endpoint table,
    `/transform` reference, provenance), CLAUDE.md §Domain (invariant now names
    scale snapping and the two permitted local ops; protected paths; verify).
- **Alternatives rejected:** hardcode the scale list in TypeScript (drifts from
  the engine — added `GET /scales` instead); reshape the engine's report into
  camelCase (more glue, two vocabularies — passed it through verbatim); dedupe
  by loudest velocity (a musical judgment the engine explicitly refused, so
  first-in-order instead); dedupe every duplicate (would delete pre-existing
  ones the engine calls the input's business — only reported slots are touched).
- **Verify:** `./verify full` exit 0 against a live bridge — 13 extension tests,
  19 bridge tests, build 34.6kb, `/health` + `/analyze` + `/transform` contract
  checks (`4 notes, 2 snapped, 1 collision(s), tie_break=previous`). Recorded in
  .harness/last-verify.json. Also exercised the real extension code path over
  HTTP end-to-end (fetchScales → transform → dedupeCollisions).
- **Open questions:** none blocking. Untested in a live Ableton — the standing
  out-of-scope gap; the dialogs' `close_and_send` path in particular has only
  been exercised by construction, not in Live. `revoice` still 501 upstream.
