# traces/

Append-only decision log — one file per merged change set, named
`YYYY-MM-DD-<slug>.md`. See `.claude/skills/provenance/SKILL.md` for the entry
format. Never edit or delete a prior trace; correct the record with a new
entry that cites the old one.
